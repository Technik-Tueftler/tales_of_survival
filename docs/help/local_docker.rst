Docker
============================

.. note::
   Documentation on this will be created once the project has 
   reached a certain level.

Running the Bot on Docker is the recommended method, as it can 
then run not only locally on the computer, but also independently on a server.